env: "local" # local, dev, prod
storage_path: "postgress://postgres:postgres@localhost/demos?sslmode=disable"
server:
  host: "localhost"
  port: "8080"
database:
  driver: "postgres"
  addr: "localhost:5432"
  username: "postgres"
  password: "postgres"
  database: "demos"
  sslmode: "disable"